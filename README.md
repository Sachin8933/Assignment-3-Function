# Python Concepts: Functions, Iterators, Generators, Lambda, Map, Reduce, and Filter

This repository includes detailed **theory questions with examples** and **practical programming exercises** to master essential Python concepts like functions, methods, iterators, generators, lambda expressions, and built-in functional tools (`map`, `reduce`, and `filter`).

---

## 📘 Theory Questions (with Examples)

### 1. What is the difference between a function and a method in Python?

- A **function** is defined using `def` and can be called independently.
- A **method** is associated with an object.

```python
def greet(): return "Hello"  # Function
"hello".upper()              # Method
